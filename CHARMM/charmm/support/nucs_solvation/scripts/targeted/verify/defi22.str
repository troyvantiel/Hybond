* defi22.str, last modified Oct/17/2001
* By Sonja M. Schwarzl & Stefan Fischer
* Definition of backbone and sidechain groups for parameter set 22
*
 
DEFI BACK1 SELE (TYPE CA .OR. TYPE N .OR. TYPE HN .or. TYPE HA*  .OR. TYPE HT*) END
DEFI BACK2 SELE (TYPE C .OR. TYPE O .OR. TYPE OT*) END
DEFI SIDE SELE .NOT. BACK1 .AND. .NOT. BACK2 END

RETURN
